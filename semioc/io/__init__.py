"""I/O helpers for contract-driven pipelines.

This subpackage is deliberately small: it centralizes strict loading and schema
validation so higher-level pipelines can remain deterministic and auditable.
"""
